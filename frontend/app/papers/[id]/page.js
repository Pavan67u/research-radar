'use client'
import {useEffect,useState} from 'react';import Link from 'next/link';import {useParams} from 'next/navigation'
const API=process.env.NEXT_PUBLIC_API_URL||'http://localhost:8000'
export default function Detail(){const {id}=useParams();const [paper,setPaper]=useState(null),[similar,setSimilar]=useState(null),[loading,setLoading]=useState(true),[error,setError]=useState('');useEffect(()=>{fetch(`${API}/papers/${id}`).then(r=>{if(!r.ok)throw Error(r.status===404?'Paper not found':'Unable to load paper');return r.json()}).then(setPaper).catch(e=>setError(e.message)).finally(()=>setLoading(false))},[id]);const findSimilar=()=>{setSimilar({loading:true});fetch(`${API}/papers/${id}/similar`).then(r=>{if(!r.ok)throw Error('Unable to find similar papers');return r.json()}).then(v=>setSimilar({items:v})).catch(e=>setSimilar({error:e.message}))};return <main className="shell"><Link className="back" href="/">← Back to papers</Link>{loading&&<div className="spinner">Loading paper...</div>}{error&&<div className="error">{error}</div>}{paper&&<><article><h1>{paper.title}</h1><div className="meta">{paper.publication_year||'Year unknown'} · {paper.authors.map(a=>a.name).join(', ')||'Unknown author'}</div><p className="abstract">{paper.abstract||'No abstract available.'}</p><div className="meta">Topics: {paper.topics.map(t=>t.name).join(', ')||'None'}</div>{paper.doi&&<p><a href={paper.doi} target="_blank">Open DOI ↗</a></p>}</article><button className="button" onClick={findSimilar}>Find similar papers</button>{similar&&<section className="similar"><h2>Similar papers</h2>{similar.loading&&<div className="spinner">Calculating similarity...</div>}{similar.error&&<div className="error">{similar.error}</div>}{similar.items?.length===0&&<div className="empty">No similar papers available.</div>}<div className="grid">{similar.items?.map(p=><article className="paper" key={p.id}><Link href={`/papers/${p.id}`}><h2>{p.title}</h2></Link><div className="meta">{p.publication_year||'Year unknown'} · {p.authors.map(a=>a.name).join(', ')}</div></article>)}</div></section>}</>}</main>}

